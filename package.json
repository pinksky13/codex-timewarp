{
  "name": "timewarp-marketplace",
  "version": "0.1.0",
  "private": true,
  "description": "Codex marketplace wrapper for the Timewarp plugin.",
  "type": "module",
  "scripts": {
    "timewarp": "npm --prefix ./plugins/timewarp run timewarp --",
    "test": "npm --prefix ./plugins/timewarp test",
    "check": "npm --prefix ./plugins/timewarp run check"
  },
  "devDependencies": {}
}
